1. Get the application URL by running these commands:
  http://{{ .Values.ingress_host }}{{ .Values.ingress_path }}

2. Test the health endpoint:
  curl http://{{ .Values.ingress_host }}{{ .Values.ingress_path }}

3. Check pod status:
  kubectl get pods -n {{ .Values.namespace }} -l "service={{ .Values.service_name }}"